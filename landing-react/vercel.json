{
    "framework": "vite",
    "buildCommand": "cd landing-react && npm install && npm run build",
    "outputDirectory": "landing-react/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}